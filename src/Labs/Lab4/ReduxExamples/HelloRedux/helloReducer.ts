import { createSlice } from "@reduxjs/toolkit";
const initialState = {
  message: "THIS IS ASSIGNMENT 4MESSAGE",
};
const helloSlice = createSlice({
  name: "ola siddhant",
  initialState,
  reducers: {},
});
export default helloSlice.reducer;
